<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/gameFragment">

    <fragment
        android:id="@+id/gameFragment"
        android:name="com.example.horsegame2.GameFragment"
        android:label="fragment_game"
        tools:layout="@layout/fragment_game" >
        <action
            android:id="@+id/action_gameFragment_to_betHistoryFragment"
            app:destination="@id/betHistoryFragment" />
    </fragment>
    <fragment
        android:id="@+id/betHistoryFragment"
        android:name="com.example.horsegame2.BetHistoryFragment"
        android:label="fragment_bet_history"
        tools:layout="@layout/fragment_bet_history" /><action android:id="@+id/action_global_betHistoryFragment" app:destination="@id/betHistoryFragment"/>
</navigation>